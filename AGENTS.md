# EDC context entrypoint

This repo ships deep architectural context generated by EDC.

Read `edc-context/index.md` first for routing, global invariants, and cross-module coupling guidance. Use `edc-context/manifest.json` as the canonical machine-readable routing and policy contract.

Runtime mode: advisory; unmatched paths use warn-allow.
